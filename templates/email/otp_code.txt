{% load fa %}PremShop
———

کد ورود شما: {{ code|fa_digits }}

این کد تا {{ ttl_minutes|fa_digits }} دقیقه اعتبار دارد و فقط یک بار قابل استفاده است.
{% if login_url %}
ورود به حساب (این لینک فقط یک بار کار می‌کند):
{{ login_url }}
{% endif %}
اگر شما این کد را نخواسته‌اید، نگران نباشید؛ بدون این کد کسی نمی‌تواند وارد
حساب شما شود. این کد را به کسی ندهید.

———
پشتیبانی: {{ support_email }}
premshop.ir
